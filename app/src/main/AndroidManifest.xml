<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rrota.pain_s">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme_to_index">
        <activity android:name=".Index"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".MainWomanActivity"
            android:label="@string/where"
            android:theme="@style/AppRed" />
        <activity
            android:name=".MainManActivity"
            android:label="@string/where"
            android:theme="@style/AppRed" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme_to_index"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Profile"
            android:label="@string/profile"
            android:theme="@style/Profile"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".History"
            android:label="@string/historyCard"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Head"
            android:label="@string/head"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Thigs"
            android:label="@string/thigs"
            android:theme="@style/AppRed" />
        <activity
            android:name=".StomachMan"
            android:label="@string/Stomach"
            android:theme="@style/AppRed" />
        <activity
            android:name=".StomachWoman"
            android:label="@string/Stomach"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Chest"
            android:label="@string/chest"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Arms"
            android:label="@string/arms"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Shank"
            android:label="@string/shank"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Neck"
            android:label="@string/neck"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Hands"
            android:label="@string/hands"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Feet"
            android:label="@string/feet"
            android:theme="@style/AppRed" />
        <activity
            android:name=".Info"
            android:label="@string/information"
            android:theme="@style/AppRed" />
        <activity android:name=".change_img" />

        <activity android:name=".ConnectActivity"
            android:label="@string/con"
            android:theme="@style/Connect"></activity>

        <activity android:name=".Drugs" android:launchMode="singleTop" android:label="Выберите лекарство"
            android:theme="@style/AppRed" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity android:name=".Analog"
            android:label="Аналог"
            android:theme="@style/AppRed"></activity>

        <activity android:name=".DrugsInfo"
            android:label="Информация о лекарстве"
            android:theme="@style/Connect"></activity>
    </application>

</manifest>